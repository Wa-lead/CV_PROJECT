from setuptools import setup, find_packages

setup(name='chemception',
      version='0.1.0',
      description='CV project chemception',
      license='MIT',
      packages=['chemception'],
      zip_safe=False)